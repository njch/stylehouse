R_Jz:
 - K: up #c scan
   D: |
       my $u = J.on.bb->{"u\t".0.1};
       my $r = {map{$_=>1} keys %$u};
       
       # bunch of keys... ^ interpret, compress knowledge
       # TODO ^make itv like two indexes, polars
       
       my $y = w $R down;
       
       my @r = grep { delete $r->{$_} } @{y.order};
       push @r, sort keys %$r;
       
       for (@r) {
            my $v = $u->{$_};
            
            my $cv = $1 if /^stuff(\d+)/;
            $cv ||= y.ordy->{$_};
            $cv ||= 4;

            J.bz->("$_\t"."0.$cv" => '       '.$v);
             # has l cursor stashed as a 1shj
             # TODO 5er grab l cursor and style
             # and what kind of cursor, fro delays/syncs
             # start culture
             # the middle
       }
 - K: mod #c make pool, synth space, want arches
   D: |
     if (ref $d eq 'ARRAY') {
         my $m = $d;
         $d = {};
         (d.t, d.thi, d.s, d.c, d.css, d.and) = @$m;
         %$d = (%$d, @{d.and}) if d.and;
     }
     d.css ||= [];
     d.css = ['%css', d.css] if !ref d.css;
     
     d.in = 1;
     if (d.over) {
         d.in += d.over;
         d.thi += d.over;
     }
     
     if (d.c) {
         my $c = d.c;
         d.c = sub {
             my ($i)  = @_;
             my $v = $i / (999 - d.in);
             my $num = $i * $v + 300;
             $num = sprintf '%.3d',$num;
             saygr "nuym $num    ";
             
             my $lastnumber = ($i =~ /(\d)$/)[0];
             my $col = join'',$lastnumber,J.ent.2,J.ent.3;

             my @gl = $num;
             # 3.3
             if (c.unico) { # some J to mod from 9
                 push @gl, ' zqeq';
                 push @gl, map{
                     unico($num+$_*10)
                     .(rand(7) > 5 ? "\n".join("", (" ") x $_ ) : "");
                 } 1..14;
             }
             if (c.jamit) {
                 push @{d.css}, '%tt#ee' => "JAMTHATFUELTANK"
             }
             if (c.fs) {
                 
                 push @{d.css}, '%css#egy' =>
                     'font-size:'.($num / 4 + 100).'%;',
             }
             # 3.4
             @gl = d.s if !@gl;

             #die wdump(2,$J);
             push @{d.css},
                '%css#olo' => 'color:#'.$col.';white-space:pre;',
                ;
             # add style tio 
             d.s = join'', @gl;
         };
     }
     
     my @is = map { int($_ * (999/d.thi)) } d.in..d.thi;
     
     my $dcss = d.css;
     for my $i (@is){ # THE TIR
         d.css = [@{$dcss}];
         d.s = d.c ? d.c->($i, $d) : d.s;
         
         my $r = d.t."$i\t0.".sprintf('%03d',$i);
         
         J.bz->($r => d.s, @{d.css}, '%css#awl', 'text-shadow:2px 2px 7px rgb(0,0,205)');
     }
 - K: geoves #c dpaw
   D: |
     J.yy.gemod = [
     # 3.24
     [zqeq => 6,  '', {unico => 1, fs=>3}],
     [sq   => 6,  '~~~'],
     [sqw  => 12, '   ~~ ~   ~ ~', '', 'color:#95d;font-size:400%;-webkit-filter:blur(1px);'],
     [sqww => 13, '~~   ~~', '',         'color:#8ff;font-size:300%;-webkit-filter:blur(1px);'],
     [dots => 14, '.', '', 'color:yellow'],
     [dotz => 22, ' z', '', 'color:black;opacity:0.4;white-space:pre;font-size:3900%;font-family:serif;'],
     # 3.3
     ];
     for my $d (@{J.yy.gemod}) {
          w $R mod[$J, $d];
     }
     # make .......................
     # the throat
      # doubling circuit starts to 6
      # to 6 - one atom at a time as far as needs subs
      
      # the = is the mod of T, wavedd
      # TODO babble like u.stuff$_ so \w+#\w+
      # bits of this world's language compress away to geometry
     
     # TODO inmod to img
     #(B.href) = $file =~ /(i\/.+$)/;
     #S.S.Z->{'background-image'} = "url('$href')";
     
     # GP PHATNESS
     my @be = grep { _.style eq 've' } w $R tvo(J=>J.geo); # poli
     for my $j (@be) {
         my $s = j.s;
         $s = join ' ', wdump(2,$s);
         J.fro->("being".j.cv."\t".j.cv =>
             $s,
             {cv=>0.7, makaz=>{ # hooky
                "/%css#jit\t"."0.1" => 'margin-left:14em;width:15em;',
             }},
            '%css', 'white-space:pre;'
            );
     }

