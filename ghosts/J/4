mod c: # pile of stuff on the ceiling
 - K: unico #c
   mod 3.3: |
      my $c = ref $cs ? $cs : {};
      my $ent = d.s =~ /^(d+)$/ ? $1 : J.ent.2;
      for my $i (1..(c.thi||24)) {
          my $entoi = $1 if ($ent * $i) =~ /(\d)$/;

          my ($b, $dd) = unico(n.um+$i* 100 * $entoi, 1);
          my $pa = $entoi > 7 ? "  " : $entoi > 3 ? " " : "";
          push @{n.gl||=[]}, $pa.$b;
      } 1..34;
 - K: numb #c
   mod 3: |
     n.um = sprintf '%.3d', ($i * $v + 5);
 - K: colourins #c
   mod 3.4: |
     $i =~ /(\d)?(\d)?(\d)?$/;
     my $col = join'',
       map { /^.*(\d+).*$/; $1 }
       map { J.ent.3 > 6 && $_ > 5 && -4 + J.ent.3 || $_ }
       map{ J.ent.1 > 6 ? $1 * (J.ent.3 / 4) :$_}
       $1,$2,$3;
       # art this shit proply ^
     d.cs.olo = 'color:#'.$col.';white-space:pre;';
 - K: fs #c MOD MOD MOD 1=stable 2=sweep # SWIM
   mod 3.3: |
     my $mod = $1 if c.fs =~ s/_(\d+)$//;
     c.fs =~ s/^0(\d+)/0.$1/; # leading zeroes, etc ssquihs
     my $siz = c.fs * 9;
     $siz -= n.um / $mod if $mod;
     d.cs.fsgy = 'font-size:'.$siz.'%;';
 - K: sh #c
   mod 3: |
     d.cs.awl = 'text-shadow:2px 2px 7px rgb(0,255,205)'
 - K: jamit #c
   mod 3: |
     d.cs.awl = "JAMTHATFUELTANK";
 - K: carbcap #c jel like language
   mod 3.9: |
     d.s = join'',@{n.gl};
     # ^ roll dough if not dough
     # ^ es also like looking for most magnity n.thee value heading for $s
 - K: dcs #c a pipeline # exp kompd style notes
   mod 7: |
     push @{d.css}, '%css#'.$_ => d.cs->{$_}
         for sort keys %{d.cs};
 - K: yinvis #c an extremity at one point
   mod 1: |
     my $v = $i / (999 - d.in);
 - K: yangco #c background language expansion
   mod 2: |
     # yang may pass arounJ komp struc intact for a while
     
R_Jz:
 - K: up #c scan
   D: |
     Rw suck */up;
 - K: cv #c mod cove
   n_D:
     oe: |
       my $v = ar.v || ar.thi || 9;
       my $wi = length($v);
       my $thi = join '', ('9') x $wi;
       my $n = 1;
       
       my $squidge;
       if (ar.off) { # offs both ends? plot shit
           $squidge = int(ar.off * ($thi / ($v))); # TODO not quite
           $n += $squidge;
           $v += $squidge; 
       }
       
       my @is = map { sprintf '%0'.$wi.'d', $_  }
           map { $_ - $squidge }
           map { int($_ * ($thi / $v)) } $n..$v;
       
       #sayyl "thehehehhe $squidge $n $v thi $thi". wdump[@is]; 
       die "numbe rdist ".wdump(2,[$v,$wi,$thi,@is]) if uniq(@is) != @is;
       @is;
       
 - K: howge #c
   D: |
     Rw tent geo;
     J.geo.bz->(@$_) for
     Rw suck */curves,
     ;
     
 - K: geoves #c dpaw
   D: |
     J.yy.gemod = [
     Rw suck */mods,
     ];
     # 6
     for my $m (@{J.yy.gemod}) {
          $m || next;
          # coul be entropated all the way to per geomodiff...
          # 7
          Rw modstick $J $m;
     }
 - K: modstick #c api layer between project and hive
   D: |
     my $d = {};
     d.t = shift @$m;
     d.thi = shift @$m;
     d.c = shift @$m;
     d.sc = shift @$m;
     say "duck food ".wdump($d);
     Rw modn $J $d;
       # yep       'color:#8ff;-webkit-filter:blur(3px);margin-left:3em'],
     
 - K: modu #c make pool, synth space, want arches
   D: |
     my $M = G.jumb.modi;
     # lop^
     d.css ||= [];
     d.css = ['%css', d.css] if !ref d.css;

     d.in ||= 1;
     d.out ||= d.thi;

     d.cs ||= {};
     d.c ||= {};
     # d.* is arg line to mod from, from */mods
     # $c=d.c is a whole little phase, language
     # this magnet would have a thrust
     my $n; # drop values through with
     my $c = d.c; # <v contation
     $c = {} if !ref $c;
     

     my $carb = sub {
         my ($i)  = @_;
         my @gl;
           
         # 6 add style tio 
          
     };

     my @is = map { int($_ * (999/d.thi)) } d.in..d.out;

     my $dcss = d.css;
     for my $i (@is){ # THE TIR
         d.css = [@{$dcss}];
         # YIN
         
         # YANG
         
         # CARBON
           
         # CITOZINE

         # 7
         my $r = d.t."$i\t0.".sprintf('%03d',$i);
         # 7.1
         J.bz->(
         # 7.2
         $r => d.s,
         # 7.25
         @{d.css},
         # 7.9
         );
     }
 - K: mod #c modi
   n_D:
     any_init: | #c
       Rw suck - */modi;
     modi: | #c shunt, suck, J maint wayfile story
       Rw Con mod modu modn;
 - K: conv #c cv to somethingness
   D: |
     $v>=3 && $v<4 ? "CARBON"
     :
     $v>=7 && $v<8 ? "CITOZINE"
     :
     die $v;
 - K: Con #c the
   D: |
     my ($the, $src, $path) = @$m;
     # or wherever thi src quags
     my $s = "$the c";
     my $J = Rw Carbonja $s;
     
       # shove togeth modn
       # get a cv sorter  that puts 3 > 32, since 3 is 3 all the way down > 3232323...
       # tesalation
       my @daw = Rw tvo $J;
       my $wtf;
       for my $j (@daw) {
           die "non$the?" if j.l ne $the; # how does that work again...
           saygr j.r;
           my $type = Rw conv $v:j.cv;
           push @{$wtf->{$type}||=[]}, j.s;
       } 
       
       my $mw = R.way.hooks->{$src};
       my $tl = {};
       for my $k (keys %$wtf) { 
           my $v = $wtf->{$k};
           $tl->{$k} = @$v;
           $v = join "\n", @$v;
           $mw =~ s/(# $k.*?)$/$1\n$v/sm || die "no $k in ...";
       } 
       saybl " __________________" for 1..4;
       say join " & ", reverse $the, reverse map{"$_*$tl->{$_}"} sort keys %$tl;
       R.way.hooks->{$path} = $mw;
     
 - K: Carbonja #c 3ish
   D: |
       my @mosd = 0->CsK({s=>$s});
       my $J = {};
       Rw suJ $J;
       for my $c (@mosd) {
           my @wsy = grep {/ /} keys %$c;
           for my $zk (@wsy) {
               my $z = $c->{$zk};
               my ($k, $v) = split ' ', $zk;
               my $D = $z;
               my $ome = c.K;
               my $com = "# $zk @ $ome  ";
               my (@l,@r);
               # if for c.cs, for implant condits # sche
               
               # lets assume K is it
                   $com .= "cs=c.K  ";
                   unshift @l, 'if (my $cs = c.'.c.K.') {';
                   $D = ind '     ', $D;
                   push @r, '}';
               
               
               $D = join "\n", $com, @l, $D, @r;
               J bz $k#$ome $v $D
           }
       }
       $J

