name: Styling
chains:
 - K: 'Wormhole'
   arr_if: |
     $G->{depth} == 1 && ref $thing eq "Wormhole";
   foreach_travel:
     each: @{$thing->{script}}
     next_thing: $each
   print: |
        $S->{i}
        
 - K: 'Line'
   arr_if: |
     $a->{K} eq "Wormhole" && ref $thing eq 'HASH' && $thing->{
   foreach_travel:
     each: sort keys %$thing
     next_thing: $thing->{$each}
   print: |
        $S->{each}
        
dials:
  max_depth: 1
  form: 42

