name: Styling
dials:
  max_depth: 7
  form: 42
hooks:

  T_end: |
    my $t = $G->{_trail} ||= [];
    my $yL = $G->{t};
    my @om = @{ $G->{o} };
    # o[ s/...
    # with i s
    # make i s... 
    push @$t, [w $yL->{i} print, $yL->{t}];
    my @no = ();
    
    return unless @$t == 10;
    
    $t = {
        t => $t,
        G => $G,
    };
    $G->timer(0.2, sub {
    $O->{O}->w(somewhere=>{thing=>$t,max_depth=>2});
    }, "bailout");
    
    

