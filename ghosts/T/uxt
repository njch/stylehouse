name: Styling
dials:
  max_depth: 7
  form: 42
hooks:

  T_end: |
    my $t = $G->{_trail} ||= [];
    my $xL = $L; 
    my $yL = $xL->{t};
    
    # o[ s/...
    # (re)make i s...
    push @$t, [w $yL->{i} print, $yL->{t}];
    my @no = ();
    
    return unless @$t == 10;
    
    $t = {
        t => $t,
        G => $G->{name},
    };
    $G->timer(0.2, sub {
        $O->{O}->w(somewhere=>{thing=>$t,max_depth=>2});
    }, "bailout");
    
    

