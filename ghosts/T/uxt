name: Styling
dials:
  max_depth: 7
  form: 42
hooks:
  any_init: |
    $G->{_trail} = [];
  T_end: |
    my @om = @{ $G->{o} };
    # o[ s/...
    # with i s
    # make i s... 
    my $t = $G->{_trail} ||= [];
    my $sL = $G->{t};
    push @$t, join "\t", $sL->{Wname}, w $sL->{i} print, $sL->{t};
    my @no = ();
    
    return unless @$t == 7;
    
    $t = join "\n", @$t;
    $G->timer(0.2, sub {
        $O->{O}->w(somewhere=>{thing=>[$G->{t},$t]});
    }, "bailout");
    
    
  fresh_init: |
    #etc
    
    
    
    
    
    
    
    
    #

