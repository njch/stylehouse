R_Jz:
 - K: oN/capstyle #c
   D: |
       my $sw;
       for (grep {!/^(travely|ori)$/} sort keys %{x.sc}) {
           $sw->{$_} = x.sc->{$_};
       }
       return unless %$sw;
       my $n = ['sc_$k', '',       {fo=>[$sw,0.7],cow=>'rgb 38f'},{}];
       Rw Mn + $n pwth;

