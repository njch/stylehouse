name: code crawl
chains:
 - K: 'lib'#c
   Ki: 'styledir'
   arr_if: |
     0 && -d $thing."/lib"
   foreach_travel:
     each: glob $thing."/lib/*"
     
 - K: 'ghosts'
   Ki: 'styledir'
   arr_if: |
     -d $thing."/ghosts"
   foreach_travel:
     each: glob $thing."/ghosts/H/*"
    
 - K: 'yaml'
   arr_if: |
     -f $thing && $thing =~ /keys/;
   foreach_travel:
     each: Way::load_yaml($thing);
dials:
  max_depth: 2
hooks:

