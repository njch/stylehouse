name: the top level molar
dials: { thuxrw: 1 }

C:
  - K: Zio
    E:
      _D: |
        my $u = G:Ϡ;
        w ion[$S, $u];
  - K: Ion
  - K: Iozn
  - K: it
    S:
      content_D: |
        "ui.uuid^".$ui->pint();
  
hooks:
  fresh_init: |
    0->GAK(U => G!U);
    0->InjC(G.GG.U => Load(<<''));
         C:
           W:
             Ss: [u, x, X]
           M:
             Ss: []
             Z: {background: rgba(0,200,150,0.3), overflow-y: scroll, overflow: visible, width: 30%, height: 100%, position: absolute, left: 40%}
  any_init: |
      my $i = 0;
      for my $u (@{G.A.n}) {
        
          sayyl $u->pi;
          last if $i++ > 20;
      }
  ion: |
    0->visTp($S, "Fun");
    0->delfrom($S);
    
    my $a = 0->TafuBl(Ion => {u=>$u});
    waylay uxd;
  percolate: |
    0->TafuB(Zio => {});
    w uxd;
  uxd: |
    my @Im = 0->scGre({});
    G.GG.U->w('ux', {Ls => [@Im]});
  event: |
    sayyl "Event relating to ".pint(L.i);
    0->visTp({L=>$L},'Fun');
    sw ($ar) if e.S;
    # try and hit E/elabourateness and then blunten until whatever
    if (e.S) {
        w $u zoum;
    }
    else {
        w $u E/_D;
    }
    waylay uxd;

