
name: entrail littered savannah
hooks:
  arr: |
    $G->{depth} = 0+@F;
  R: |
    # multitudable R
    # splits above here through tractor
    $G->RW;
    if (ref $for eq "Ghost") {
        $for->T->{TT} = $G;
    }
    else { die }
    # yeah

