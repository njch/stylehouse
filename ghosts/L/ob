
name: entrail littered savannah
include: W/tractor
tractors:
  - K: ob
    arr_if: |
      1
    first_time: |
      T.L.depth = 0+@F;
      my $t = $thing->{thing};
      w stitch_stack;
      my $s = $t->[1];
      my $p = w $s print if ref $s eq "Way";
      $t->[0] .= " $p" if $p;
      
hooks:

  stitch_stack: |
    # etc
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    #ye
  R: |
    # multitudable R
    # splits above here through tractor
    0->RW($G);
    if (ref $for eq "Ghost") {
        $for->T->{TT} = $G;
    }
    else { die }
    # yeah

