
name: entrail littered savannah
hooks:
  arr: |
    $G->{depth} = 0+@F;
    my $t = $thing->{thing};
    my $s = $t->[1];
    my $p = w $s print if ref $s eq "Way";
    $t->[0] .= " $p" if $p;
  R: |
    # multitudable R
    # splits above here through tractor
    $G->RW;
    if (ref $for eq "Ghost") {
        $for->T->{TT} = $G;
    }
    else { die }
    # yeah

