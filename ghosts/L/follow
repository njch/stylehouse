
name: trail of ways...
tractors:
  - K: Thing
    arr_if: |
      1
    B_: |
      $ar->{name} if $ar->{name};
      $thing
    print: |
      $S->{B}->{_};
hooks:
  recoded_init: |
    my $thing = G(tractor)->w('random');
    $O->w('somewhere', { thing => $thing } );
  arr: |
    my $S = Arr[$thing];

    # etc;
    

