
name: trail of bits
tractors:
  - K: Thing
    arr_if: |
      1
    B_: |
      $thing
    print: |
      $S->{B}->{_};
hooks:
  recoded_init: |
    $O->w('somewhere', { thing => [@Flab] } );
  arr: |
    my $S = Arr[$thing];
    # etc;
    

