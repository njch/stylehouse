
name: trail of bits
tractors:
  - K: Thing
    arr_if: |
      1
    B_: |
      $thing
    print: |
      $S->{B}->{_};
hooks:
  recoded_init: |
    my $thing = G(tractor)->w('random');
    $O->w('somewhere', { thing => $thing } );
  arr: |
    my $S = Arr[$thing];
    # etc;
    

