
name: trail of ways...
include: W/tractor
tractors:
  - K: Thing
    arr_if: |
      1
    B_: |
      $ar->{name} || $ar->{code} || $thing
    print: |
      $B->{name} || $B->{thing};
    to_travel_this: |
      $B->{code} ? $B->{code}->() : $B->{thing};
hooks: {}

