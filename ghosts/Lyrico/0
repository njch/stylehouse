name: Lyricoso writoro
hooks:
  load_ways_post: |
    say 'yeha!';
    $O->somewhere() if $G->{click}++;
  bang: |
    say "Something!\n" for 1..20;
chains:
 - at_thing: |
     $G->{depth} == 0 && ref $thing && "$thing" =~ /=HASH/
   as: HASH
   print: "'Lyrico'"
something:
  else: 1

