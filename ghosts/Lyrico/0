name: Lyricoso writoro
hooks:
  load_ways_post: |
    say 'yeha!';
    $O->somewhere() if $G->{click}++;
  bang: |
    say "Something!\n" for 1..20;
  etc: |
    my $what = $self->{lyrics};

    $what = '!';
    $self->{hostinfo}->ravel(
        $self->{T},
        $what,
        $self->{wormhola}
    );
chains:
 - as: HASH
   note: ' { owner => "yeayh" } '
   ref: Lyrico
something:
  else: 1

