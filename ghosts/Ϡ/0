name: Ϡ
etc: intoraction... magnetic fishing rod, guy playing guitar cave art
GG:
 - K: ool
   way: C/ool
 - K: ux
   W_Ss: [X]
   way: T/ux
C:
  - K: GG
  - K: nothing
hooks:
  V:
    ux:
      styles: cardboard ux
  styles:
    cardboard: |
      qw'position:absolute color:#3bc', 'border:2px solid #003'
      
    ux: |
      qw'width:133% height:100% font-size:10pt opacity:1 pointer-events:none'
  Arr: |
    my ($what) = keys %$ar;
    my $u = 0->TafuB($what => $ar);
    die sw([Ϡwhatnow => $u]);
  keys:
    n: |
      G:Ϡ ->_0('V/blur');
    N: |
      Say "TOGIN";
      G:Ϡ ->_0('V/toginvis');
  fresh_init: |
    w scribe_js;
  scribe_js: |
    $G->timer(0.3, sub { w scribe_js; });
    
    my $new = G.js || [];
    return unless @$new;
    G.js = [];
    
    my $now = $H->hitime();
    G.jslast ||= $now;
    my $d = sprintf("%.3f",$now - G.jslast);
    G.jslast = $now;
    
    my $script = join"","[$d,function(){", @$new, "}],\n";
    write_file("o/o", {append=>1}, Hostinfo::encode_utf8($script));
    
  any_child_recoded_init: |
    w wux;
  recoded_init: |
    w wux;
  uW: |
    Say "from u.G.name is u.K for ".ki(u.B);
    u.K eq 'W' || die "NOW";
    
    if (u.G eq G.GG.ux) {
        sayyl "Ϡ takeover";
        G.V.ux->takeover(u.html);
    }
    else {
        sayyl "Ϡ <into< G.GG.ux.name"; # slip somewhere
        G.GG.ux->w(ux => {Ls => [u.Li]});
    }
  ux: |
    for my $GG (@{G.GGs}) { # could ar into GG=$K orso
        sayre "RWRWRWRW GG.name";
        for my $ux ($GG->Gf("T/ux")) {
            0->RW($ux);
        }
        0->RW($GG);
        $GG->w('percolate');
    }
    w wux;
    saybl "ux ".$G->F_delta();
  wux: |
    for my $GG (@{G.GGs}) {
        $GG->w('uxd');
    }

