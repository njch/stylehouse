R_Jz:
 - K: ps #c fo9od
   n_D:
     yJup: |
       qw'ps oci ';
     curves: |
       ["y 0" => 0.9],
       ["x 0" => 0.12],
       ["spun 0" => 0.75],  
       ["curve 0.1" => -0.024],
       ["zoom 0" => 0.3],

       #["curve 0.4" => 0.3],
     up: |
       Rw ii/psfaus;
 - K: ii/psfaus #c ii = indigo, pi = carving, wt = maneuvrable type 
   D: |
       my @l = `ps faux | grep perl`;
       my $wtf = [];
       for my $l (@l) {
           chomp $l;
           next if $l =~ /grep perl/;
           my @r = split /\s+/, $l, 11;
           my $cs = [@r[0..3,9,10]];
           my $o = Rw pul $cs user pid cpu mem time cmd;
           
           push @$wtf, $o;
       }
       my $M = [];
       for my $f (@$wtf) {
           push @$M,
               ["SOI", '',
               {fo=>[$f,0.8,'psfacts'],i=>1}, {co=>'4fc',zi=>10,ws=>'p'}],
               
       }
       my $in = 0.1;
       my $out = 0.9;
       my $k = 'psfaus';
       
       my $n = $M->[3]->[2];
       
       Rw mo/pwth $J $M $in $out $k;
       #Rw jin ollox $in:n;
 - K: pi/psfacts #c
   D: |
        $_ =  u.cmd;
        my ($style,$name) =
            /l\/S\.pm (.+)$/ ? ('hut', $1)
            :
            /stylehouse\.pl$/ ? ('h00s', $_)
            :
            /\.\/ps/ ? ('ps', $_)
            :
            ("weird", $_);
        
        if ($style ne 'hut') {
            $name =~ s/^[| ]+//;
        }
        if ($style eq 'ps') {
            $name =~ s/\/usr\/bin\/perl //;
        }
        
        my $M = []; # HMMMMM may wannsa J?
        
        Rw wt/stylename $M $style $name; 
        
        Rw wt/cpu $M $cpu:u.cpu;
        Rw wt/mem $M $mem:u.mem;
        
        warn "heaps of mem on $style $name  (u.mem)" if u.mem > 20;
        my $V = 1;
        
        Rw mo/pwth $J $M $V;
        
        # J.fo->($u,0.3); # fo HASH complicato
 - K: wt/stylename #c  TODO return _M_ brigy dep text jostle
   D: |
       my $sc = {}; # carbon flux $style
       my $nc = {}; # carbon flux $name
       
       if ($style eq 'hut') {
           sc.co = 'fc7';
           sc.fs = 14;
       }
       if ($style eq 'h00s') {
           sc.co = '007';
           sc.sha = '3 1 2 cff6';
       }
       if ($style eq 'ps') {
           sc.co = '804';
           sc.sha = '3 1 2 fc77';
       }
       # ^ sheety
       
       nc.co ||= '4fc';
       if ($style eq 'hut') {
           nc.co = 'ff0';
           nc.fs = 29;
       }
       if ($name eq '\_ perl stylehouse.pl') {
           $name = 'stylehouse.pl';
           nc.fs = 8;
       }
       sc.sha = '1 2 6 000';
       nc.sha = '1 2 6 000';
       
       Rw wtf $M style $s:style $sc;
       Rw wtf $M name $s:name $sc:nc;
 - K: wt/cpu #c  # TODO make fs smooth, atomical curve compu
   D: |
       my $sc;
       sc.co = 'eb0';
       sc.fs = $cpu<0.5?4:$cpu<2?7:$cpu<4?14:18;
       sc.sha = '1 2 6 000';
       Rw wtf $M cpu $s:cpu $sc;
 - K: wt/mem #c
   D: |
       my $sc;
       sc.co = '8e0';
       sc.co = 'f30' if $mem > 20;
       sc.fs = $mem<2?4:$mem<5?7:$mem<12?14:28;
       Rw wtf $M cpu $s:mem $sc;

