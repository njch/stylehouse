Travel:
   yJ: |
     'aww splain'
   M: |
     #Rw Emu;
     #Jn(speed => 1
     Jn(zwelch => 0.3, 'TRAVEL');
   non: |
      to overload the filesystem
      a bz ring
      make a whole sub
      easy
      its an oversight
      a ribbing
        texture directives
          way over
               
splain:
   yJ: |
     my $A = J.A = J.most.A = {}; 
     A.time = sub{ 
         my ($J,$wa) = @_;
         J.A.ztime >= $wa ? 1 : do {
             warn gp($J)." Wants time, $wa";
             acum $A, fo => $J;
             0;
         };
     };
     'splain';
Jeight: | # oo mod
       my $ar = {%{d.c}};
       ar.J=d.s;
       
       return d.s if !A.hav && A.jr&ok-9;
       my $n = A.hav ? sub{} : A.jr&ok-9,9;
       
       Rw around;
       
       $n->();

