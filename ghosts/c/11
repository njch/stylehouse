
Chl: | # W/js outputting # Feb 9 2016 (Elt also works)
    my $n = Rw Dwive 8;
    # flappy, should not Wormhole the sc.X spiral
    # TODO eine client end acum
    
    if ($n == 0) {
        -d "w/jest" &&
        `cd w/jest; rm testChangAng testChangAngGoil tesooyl testylate`;
        n testChang =>W=>'%test:1,West:1'
    }
    elsif ($n == 1) {
        -d "w/jest" &&
        `cd w/jest; rm testChangAng testChangAngGoil tesooyl testylate`;
        n testChang =>W=>'%test:2,West:2',{l=>'some chadded, indexed'};
    }
    elsif ($n == 2) {
        n testChang =>W=>'%test:3,West:3'
    }
    else {
        ok(0,'runto much'.$n);
    }
testChang: |
    #u slowmoving
    u Traweil
    n \'W:testChangAng,isle:jest'
    
testChangAng: |
    y.at.sq = 'jsle';
    # Traweil/isle 747 formulates our sc.X after oscwaver jsle needs it
    # could be done in osc?
    (y.at.X) = map{_.M->[0]->{sc}->{X}}
    n testChangAngGoil
    
testChangAngGoil: |
    n tesooyl
    n testylate
tesooyl: |
    u alljs
    #
    n yoolifive =>5=><<''
        // ties up seque
        var z = G&qo;
        z ||= G&testiW,'four','of',te;
        z.length and G&$z.j,3
        // queueye
    
    yolify 45: %args:e j i
        // mulls
    
testylate: |
    u alljse
    n \'%nk:c'
    
    n testiW =>28=><<''
        // Tiesto
        
    
    n testnobody =>31=><<''
        A.on = $('#'+C.c.nobody);
        !$(A.on).length and a.c("no nobody at "+C.c.nobody);
    
    J.se.runs++ &&
    Rw testylater;
testylater: |
    n testiW =>28=><<''
        A.on = $('#'+C.c.W);
        $.extend(A,W.A);
    
    n testspeakeasy =>39=><<''
        // somebody at
    

