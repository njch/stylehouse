use strict;
use warnings;
use FindBin qw($Bin);
use lib "$Bin/othlia";
use Down::Goodat;
use Down::Tides;
use G;
use Ise::KnowTime;
use Ise::Rob;
use Ise::Shelf;
our $A = {};
$A->{I} = {};
%{$A->{I}} = (%{$A->{I}}, %$_) for
   $Down::Goodat::A,   $Down::Tides::A,   $G::A,   $Ise::KnowTime::A,   $Ise::Rob::A,   $Ise::Shelf::A;
sayyl wdump $A;
