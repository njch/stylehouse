use strict;
use warnings;
use FindBin qw($Bin);
use lib "$Bin/othlia";
use G;
use Goat::Goodat;
use Goat::Gooso;
use Goat::Tides;
use Ise::IStuff;
use Ise::KnowTime;
use Ise::Rob;
our $A = {};
$A->{I} = {};
%{$A->{I}} = (%{$A->{I}}, %$_) for
   $G::A,   $Goat::Goodat::A,   $Goat::Gooso::A,   $Goat::Tides::A,   $Ise::IStuff::A,   $Ise::KnowTime::A,   $Ise::Rob::A;
sayyl wdump 3, $A;
