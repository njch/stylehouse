my $wire = 'from';
return unless C.sc.z;
for my $zi (@{C.sc.z}) {
    die 'NOZ' if !C.sc.z;
    my $from = [grep{defined}@{C.c->{$wire}||[]},$C];
    my $c = {c=>{$wire=>$from}};
    # the merging 
    push @{A.N}, {EC=>[[$zi],$c]};
}
