my $M = J.UG||die"stuppug";
for (e&C) {
    # y.unit 
    my $id = _.sc.id;
    if (grep{_.sc.id eq $id}@$M) {
        T.oke = 1;
    }
    else {
        Mn(_.t=>''=>"Thi _.t",{id=>$id,swy=>'de',nspc=>0.3});
        T.oke = 1; # immedia
    }
}
